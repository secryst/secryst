# 08 — Map DSL: neutral `vocalize(...)` function (DEFERRED, coordinated)

interscript-ruby stdlib exposes BOTH secryst() and rababa() as map
functions — project names fossilized as linguistic operations. Replace
with ONE neutral function: vocalize(output, model: "ara-diac-r5")
resolving via the models.yaml index; implementation (secryst crystal)
selected by the host runtime.

DEFERRED: must ride the .imp -> .isc migration codemod (289 maps) —
renaming functions standalone would break parity tests and the
deep-equivalence work. Execute with the .isc campaign. Owners: this
rebrand campaign hands the decision to the .isc migration stream.
Status: DEFERRED (decision recorded; execution with .isc migration)
