# 09 — Registry + brand claims, memory updates

- PyPI `secryst`: NOT yet claimed — upload 403 (stale token); artifacts ready in ~/src/secryst/secryst-py/dist/. Needs a fresh PyPI token from the account owner.
- npm org `@secryst` + `secryst (bare npm name)`: BLOCKED on npm auth — needs
  `npm login` (interactive) by user, then `npm org create`/publish from
  secryst-ts (05). Tarball is ready.
- ml-models README trio naming: update "Ruby (secryst gem) / TS
  (@interscript/ml)" -> secryst crystal family (Python secryst, Ruby
  secryst, TS secryst (bare npm name)) — with 02.
- Memory: secryst-pr44-layout.md updated to post-reconnect reality.
- GitHub: secryst org profile (description: "Crystal runtimes that
  reveal the hidden reading of a script — neural vocalization/G2P for
  Interscript") + interscript org description mention.

Status: DONE except npm publish (user auth required)
